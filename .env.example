# UNED Group Links - Environment Variables Example
# Copy this file to .env.local for local development
# These variables should be set in Vercel for production

# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# (Accessible in React - must start with REACT_APP_)
# ============================================

# Cards Data (JSON string) - Managed automatically by the admin panel via Vercel API
# This will be set automatically when you update cards through the admin dashboard
# Example format: '[{"title":"Academic Calendar","route":"https://...","iconSVG":"<svg>...</svg>"}]'
REACT_APP_CARDS_DATA=

# Admin Credentials
# Set these to override the default hardcoded values in SuperAdminLogin.jsx
REACT_APP_ADMIN_EMAIL=your-email@example.com
REACT_APP_ADMIN_PASSWORD=your-secure-password-here

# Admin Secret for API Authentication
# This is used to authenticate API requests from the admin panel to the serverless function
# Generate a strong random string for production
REACT_APP_ADMIN_SECRET=your-random-secret-key-change-this-in-production

# ============================================
# SERVERLESS FUNCTION ENVIRONMENT VARIABLES
# (Only accessible server-side in /api functions)
# ============================================

# Vercel API Token
# Generate from: https://vercel.com/account/tokens
# This token is used by the serverless function to update environment variables
VERCEL_TOKEN=your_vercel_api_token_here

# Vercel Project ID
# Find in: Vercel Dashboard → Your Project → Settings → General
VERCEL_PROJECT_ID=your_project_id_here

# Vercel Team ID (Optional - only needed for team accounts)
# Find in: Vercel Dashboard → Your Team → Settings
VERCEL_TEAM_ID=

# Vercel Project Name
# The name of your project in Vercel
VERCEL_PROJECT_NAME=uned-group-links

# Vercel Git Reference (Branch)
# The git branch to deploy (usually 'main' or 'master')
VERCEL_GIT_COMMIT_REF=main

# Admin Secret (Server-side copy)
# Must match REACT_APP_ADMIN_SECRET for API authentication
ADMIN_SECRET=your-random-secret-key-change-this-in-production

# ============================================
# SETUP INSTRUCTIONS
# ============================================

# 1. LOCAL DEVELOPMENT:
#    - Copy this file to .env.local
#    - Fill in at least REACT_APP_ADMIN_EMAIL and REACT_APP_ADMIN_PASSWORD
#    - REACT_APP_ADMIN_SECRET can use a simple value for development
#    - Vercel-related variables are not needed for local development

# 2. VERCEL PRODUCTION:
#    - Go to Vercel Dashboard → Your Project → Settings → Environment Variables
#    - Add all variables listed above
#    - For VERCEL_TOKEN:
#      a. Go to https://vercel.com/account/tokens
#      b. Create a new token with full access
#      c. Copy the token (you won't see it again!)
#    - For VERCEL_PROJECT_ID:
#      a. Go to your project settings
#      b. Look under "General" section
#      c. Copy the Project ID
#    - For ADMIN_SECRET and REACT_APP_ADMIN_SECRET:
#      a. Generate a strong random string (e.g., using a password manager)
#      b. Use the SAME value for both variables
#    - REACT_APP_CARDS_DATA will be automatically managed by the admin panel

# 3. SECURITY NOTES:
#    - Never commit .env.local to git (it's in .gitignore)
#    - Use strong, unique values for all secrets in production
#    - Rotate VERCEL_TOKEN periodically for security
#    - The ADMIN_SECRET must match between client and server

# 4. HOW IT WORKS:
#    - Admin makes changes through the dashboard
#    - Dashboard calls /api/update-cards with the new data
#    - Serverless function uses VERCEL_TOKEN to update REACT_APP_CARDS_DATA
#    - Vercel automatically triggers a new deployment
#    - After ~60-120 seconds, changes are live
#    - HomePage reads from the updated REACT_APP_CARDS_DATA

# ============================================
# TROUBLESHOOTING
# ============================================

# If deployment fails:
#   - Check that VERCEL_TOKEN has the correct permissions
#   - Verify VERCEL_PROJECT_ID matches your project
#   - Ensure ADMIN_SECRET matches REACT_APP_ADMIN_SECRET

# If admin login fails:
#   - Verify REACT_APP_ADMIN_EMAIL and REACT_APP_ADMIN_PASSWORD are set
#   - Check browser console for errors

# If cards don't update:
#   - Check browser console for API errors
#   - Verify the deployment completed successfully in Vercel dashboard
#   - Try a hard refresh (Ctrl+Shift+R) after deployment completes
